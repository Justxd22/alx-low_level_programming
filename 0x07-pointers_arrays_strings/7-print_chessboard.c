#include "main.h"

/**
 * print_chessboard - prints chess board
 * @a: array
 * No Return
 */
void print_chessboard(char (*a)[8])
{
	unsigned int x;

	for (x = 0; s[x] != '\0'; x++)
	{
		if (s[x] == c)
		{
			return (s + x);
		}
	}
	if (s[x] == c)
	{
		return (s + x);
	}
	return (0);
}
