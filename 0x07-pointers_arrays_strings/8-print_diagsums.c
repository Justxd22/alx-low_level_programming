#include "main.h"

/**
 * print_diagsums - prints sum of diagonals
 * @a: array
 * @size: int size
 * No Return
 */
void print_diagsums(int *a, int size);
{
	unsigned int x;

	for (x = 0; s[x] != '\0'; x++)
	{
		if (s[x] == c)
		{
			return (s + x);
		}
	}
	if (s[x] == c)
	{
		return (s + x);
	}
	return (0);
}
